# Execução operacional — FG-010 Cadastro de clientes

**Slug:** `fg-010`  
**Branch sugerida:** `feature/fg-010`  
**ZIP:** `docs/actions-exec/fg-010.zip`  
**SHA-256 do ZIP:** `1627d671e049762500e4b8122fb11d7cd6e04ab7194d8a92f67281e3c9377ccf`  
**Data:** 2026-04-27

## 1. Escopo entregue

Pacote autocontido para implementação/adaptação da funcionalidade **FG-010 — Cadastro de clientes**.

Inclui:

- Contrato OpenAPI.
- Backend TypeScript modular com tipos, validações, autorização, service, auditoria, eventos e rotas.
- Frontend Flutter/Dart com modelo, repository e telas de listagem/formulário.
- Fragmentos Firestore Rules e indexes.
- Migration SQL de referência.
- Testes-base de validação.
- Script seguro de validação de pacote.

## 2. Premissas

- Nenhum repositório-fonte foi anexado nesta conversa.
- Os arquivos foram gerados em pacote adaptável; caminhos e imports devem ser ajustados ao repositório real.
- Autenticação, sessão, grants, permissões, auditoria e logger existentes devem ser reutilizados.
- `tenantId`, `actorId`, `empresaId` e `filialId` devem vir do contexto autenticado.
- Não usar service account no frontend.
- Não gravar segredos, CPF/CNPJ completo ou payload bruto em logs.
- Regras fiscais externas e política LGPD final exigem validação humana.

## 3. Arquivos no ZIP

- `codigo-fonte/README.md`
- `codigo-fonte/backend/firebase/firestore.indexes.fragment.json`
- `codigo-fonte/backend/firebase/firestore.rules.fragment`
- `codigo-fonte/backend/typescript/src/modules/customers/customer.audit.ts`
- `codigo-fonte/backend/typescript/src/modules/customers/customer.authz.ts`
- `codigo-fonte/backend/typescript/src/modules/customers/customer.controller.ts`
- `codigo-fonte/backend/typescript/src/modules/customers/customer.events.ts`
- `codigo-fonte/backend/typescript/src/modules/customers/customer.repository.ts`
- `codigo-fonte/backend/typescript/src/modules/customers/customer.routes.ts`
- `codigo-fonte/backend/typescript/src/modules/customers/customer.service.ts`
- `codigo-fonte/backend/typescript/src/modules/customers/customer.types.ts`
- `codigo-fonte/backend/typescript/src/modules/customers/customer.validators.ts`
- `codigo-fonte/backend/typescript/test/customers/customer.validators.spec.ts`
- `codigo-fonte/contracts/openapi/fg-010-customers.openapi.yaml`
- `codigo-fonte/database/sql/20260427_fg010_customers.sql`
- `codigo-fonte/frontend/flutter/lib/features/customers/data/customers_repository.dart`
- `codigo-fonte/frontend/flutter/lib/features/customers/domain/customer.dart`
- `codigo-fonte/frontend/flutter/lib/features/customers/presentation/customer_form_page.dart`
- `codigo-fonte/frontend/flutter/lib/features/customers/presentation/customers_list_page.dart`
- `codigo-fonte/frontend/flutter/test/features/customers/customer_validation_test.dart`
- `codigo-fonte/tools/validate-fg010-package.sh`

## 4. Comandos seguros para GitHub Actions

O bloco abaixo valida o ZIP, impede path traversal e confere a presença da pasta `codigo-fonte/`. Ele não aplica alterações no repositório automaticamente.

```bash
<!-- actions-exec:run -->
set -euo pipefail

slug="fg-010"
zip_path="docs/actions-exec/${slug}.zip"
extract_dir="tmp/actions-exec/${slug}"

echo "Validando artefato ${zip_path}"
test -f "${zip_path}"

python - <<'PY'
from pathlib import Path
import zipfile
import hashlib

slug = "fg-010"
zip_path = Path("docs/actions-exec") / f"{slug}.zip"
data = zip_path.read_bytes()
print("sha256=", hashlib.sha256(data).hexdigest())

with zipfile.ZipFile(zip_path) as zf:
    names = zf.namelist()
    if not names:
        raise SystemExit("ZIP vazio")
    for name in names:
        p = Path(name)
        if p.is_absolute() or ".." in p.parts:
            raise SystemExit(f"Caminho inseguro no ZIP: {name}")
        if not name.startswith("codigo-fonte/"):
            raise SystemExit(f"Entrada fora de codigo-fonte/: {name}")
    required = [
        "codigo-fonte/contracts/openapi/fg-010-customers.openapi.yaml",
        "codigo-fonte/backend/typescript/src/modules/customers/customer.validators.ts",
        "codigo-fonte/backend/typescript/src/modules/customers/customer.service.ts",
        "codigo-fonte/frontend/flutter/lib/features/customers/domain/customer.dart",
        "codigo-fonte/database/sql/20260427_fg010_customers.sql",
    ]
    missing = [item for item in required if item not in names]
    if missing:
        raise SystemExit("Arquivos obrigatórios ausentes: " + ", ".join(missing))
print("ZIP FG-010 validado com sucesso.")
PY

mkdir -p "${extract_dir}"
python - <<'PY'
from pathlib import Path
import zipfile

slug = "fg-010"
zip_path = Path("docs/actions-exec") / f"{slug}.zip"
extract_dir = Path("tmp/actions-exec") / slug
with zipfile.ZipFile(zip_path) as zf:
    zf.extractall(extract_dir)
print(f"Extraído para {extract_dir} somente para inspeção.")
PY

bash "${extract_dir}/codigo-fonte/tools/validate-fg010-package.sh" "${extract_dir}/codigo-fonte"

git status --short || true
```

## 5. Checklist de aplicação manual no repositório real

- [ ] Criar branch `feature/fg-010`.
- [ ] Conferir alterações existentes com `git status`.
- [ ] Extrair ZIP em diretório temporário.
- [ ] Copiar arquivos para os caminhos reais preservando padrões do projeto.
- [ ] Ajustar imports, injeção de dependência, rotas e providers.
- [ ] Integrar service ao repositório real.
- [ ] Integrar auditoria e eventos de domínio existentes.
- [ ] Aplicar migration SQL ou indexes/rules Firestore em ambiente controlado.
- [ ] Rodar lint/typecheck/testes/backend/frontend.
- [ ] Revisar Security Rules/policies com foco em tenant isolation.
- [ ] Revisar LGPD: minimização, retenção, exclusão e mascaramento.
- [ ] Validar fluxo manual mobile/desktop.
- [ ] Commitar com mensagem clara.
- [ ] Abrir PR com evidências.

## 6. Comandos sugeridos no repositório real

> Ajustar scripts conforme `package.json`, `pubspec.yaml`, monorepo ou CI real.

```bash
git status
git checkout -b feature/fg-010
git pull --ff-only || true

# Extrair em diretório temporário para revisão, não diretamente sobre o código.
mkdir -p tmp/actions-exec/fg-010
python -m zipfile -e docs/actions-exec/fg-010.zip tmp/actions-exec/fg-010

# Após copiar/adaptar manualmente:
npm run lint --if-present
npm run typecheck --if-present
npm test --if-present
flutter test || true

git status
git add .
git commit -m "feat(customers): implement FG-010 customer registry"
git push -u origin feature/fg-010
```

## 7. Riscos e pendências

- Stack real não foi inspecionada; arquivos são adaptáveis.
- Integrações fiscais/financeiras dependem de contratos reais.
- Política LGPD de retenção, anonimização e exclusão requer validação humana.
- Regras de crédito e bloqueio dependem do financeiro.
- Testes devem ser executados no repositório real após adaptação.

## 8. Validação local realizada

- Planilhas lidas com sucesso.
- FG-010 identificado na planilha de requisitos.
- 3 documentos criados.
- Markdown operacional criado.
- ZIP criado com entradas apenas sob `codigo-fonte/`.
- SHA-256 calculado.
- Arquivos verificados por manifesto.
